# specs/

Your dashboard specs live here: one JSON file per dashboard, and the file is
the source of truth (version it, diff it, re-apply it).

Start a new one from the example in the project README, from
`tests/fixtures/sales_overview.json`, or with the `superset-dashboard`
Claude Code skill ("build me a dashboard showing ...").
